import { describe, it, expect } from 'vitest';

describe('HopeCard-Web', () => {
  it('should have correct project name', () => {
    expect('HopeCard').toBeTruthy();
  });

  it('should pass basic math', () => {
    expect(1 + 1).toBe(2);
  });
});
